spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true # H2 ?? ???
      path: /h2-console # ?? ?? ??
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true # SQL ?? ??
    properties:
      hibernate:
        format_sql: true